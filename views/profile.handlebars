<header class="row">
  <h2>Profil</h2>
  <a class="btn right" href="{{backHref}}">
    {{backText}}
  </a>
</header>

<section class="card" style="text-align:center">
  <div
    style="width:120px;height:120px;border:2px solid #222;border-radius:50%;margin:0 auto 8px;"
  ></div>
  <div>{{user.name}}</div>
  <button class="btn" style="margin-top:8px">Edit</button>
</section>

<section class="card list">
  {{#menuItems}}
    <div class="item">{{.}}</div>
  {{/menuItems}}
</section>
